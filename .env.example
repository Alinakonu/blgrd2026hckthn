# SoilShift — copy these into Render / local .env

# Force cached Vojvodina parcels (demo-safe). Set to 0 to prefer live SoilGrids + Open-Meteo.
USE_FIXTURES=1

# Optional later:
# NEXT_PUBLIC_CONVEX_URL=
# OPENAI_API_KEY=
# EXA_API_KEY=
# FIRECRAWL_API_KEY=
