"use client";

/** future: LinkedIn agritech company intel */
export default function LinkedInIntelStub() {
  return (
    <aside className="border border-dashed border-[var(--rule-strong)] bg-black/20 px-4 py-4 text-sm backdrop-blur-sm">
      <p className="text-[11px] uppercase tracking-[0.2em] text-[var(--muted)]">
        future: LinkedIn agritech company intel
      </p>
      <p className="mt-2 leading-relaxed text-[var(--ink-soft)]">
        Stub only — later this panel lists nearby agritech companies, co-ops, and
        extension partners for the pinned field. Hook:{" "}
        <code className="text-[var(--accent)]">/api/linkedin-intel</code> ·{" "}
        <code className="text-[var(--accent)]">src/lib/linkedin-stub.ts</code>
      </p>
    </aside>
  );
}
